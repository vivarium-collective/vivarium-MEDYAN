[tox]
skipsdist = True
envlist = py38, lint
requires = tox-conda

[testenv:lint]
deps =
    .[test]
commands =
    flake8 vivarium_medyan --count --verbose --show-source --statistics
    black --check vivarium_medyan

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    .[test]
conda_deps =
    readdy
conda_channels =
    readdy/label/dev
    conda-forge
commands =
    pytest --basetemp={envtmpdir} --cov-report html --cov=vivarium_medyan vivarium_medyan/tests/
